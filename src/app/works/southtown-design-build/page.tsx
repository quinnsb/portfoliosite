import type { Metadata } from "next";
import SouthtownMockPage from "../../all-work/southtown-design-build/page";

export const metadata: Metadata = {
  title: "Southtown Design+Build — Quinn Brewer",
  description: "An example editorial project landing page concept.",
};

export default SouthtownMockPage;
