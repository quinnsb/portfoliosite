<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title | default("Brewer Research & Design") }}</title>
  <meta name="description" content="{{ description | default('A personal archive of rankings, writing, and projects.') }}">
  {% include "partials/head-meta.njk" %}
  <link rel="stylesheet" href="/assets/css/main.css">
</head>
<body>

  {% include "partials/header.njk" %}

  <main class="container py-8">
    {{ content | safe }}
  </main>

  {% include "partials/footer.njk" %}

</body>
</html>
